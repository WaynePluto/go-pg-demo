# 上下文

- `migration/db` 数据库迁移目录，数据库字段以此目录下为准。
- golang-sqlx操作数据的规则参考`promot/rules/sqlx.md`。


# 需求
在pkgs目录下新增一个函数。函数功能是
1. 检索是否存在username为admin的用户，如果没有则创建，admin用户的密码为`4a7d1ed414474e4033ac29ccb8653d9b`;
2. 检索是否存在name为root的角色，如果没有则创建；
3. 确保admin用户拥有root角色；
4. 确保root角色拥有所有的权限；

**注意**
- 只用实现这个函数，函数只需要返回error，不需要返回其他内容；
- 函数相关的注释、返回信息等描述信息，都使用中文；